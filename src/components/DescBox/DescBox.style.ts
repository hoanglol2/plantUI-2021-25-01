import {StyleSheet} from 'react-native';
export default StyleSheet.create({
    wrapper: {
        width: '50%',
        alignItems: 'center',
        justifyContent: 'center',
    },
    text: {
        color: '#62636a',
        fontWeight: 'bold',
        fontSize: 17
    }
});